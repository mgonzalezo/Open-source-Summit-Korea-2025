apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: carbon-mcp

resources:
- namespace.yaml
- rbac.yaml
- configmap.yaml
- deployment.yaml
- service.yaml

commonLabels:
  project: kepler-oss-korea-2025
  component: carbon-mcp

images:
- name: your-registry/carbon-kepler-mcp
  newTag: latest
