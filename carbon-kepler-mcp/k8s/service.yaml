apiVersion: v1
kind: Service
metadata:
  name: carbon-mcp-server
  namespace: carbon-mcp
  labels:
    app: carbon-mcp-server
spec:
  type: NodePort
  selector:
    app: carbon-mcp-server
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    nodePort: 30800
    protocol: TCP
